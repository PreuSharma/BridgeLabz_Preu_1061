//34. Create a function that returns an array with words inside a text.

let text="welcome to chitkara explore your potential"
function abcd(text)
{
    finalArr=text.split(" ")
    return finalArr;
}

console.log(abcd(text))